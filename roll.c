#include <stdio.h>
int main()
{
 int roll_no, age;
 printf("Enter the roll number:");
 scanf("%d",&roll_no);
 printf("Enter the age:");
 scanf("%d",&age);
 printf("The roll number is %d\n",roll_no);
 printf("The age is %d\n",age);
 return 0;
 }
